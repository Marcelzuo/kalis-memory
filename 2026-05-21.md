
## 网络配置发现

问题：`web_search`默认用Google，在大陆无法访问。微信文章需要JS渲染，直接抓取拿不到内容。

测试结果：
- ✅ 国内可用：百度(200)、搜狗(200)、360(301)、cn.bing.com(200)
- ❌ 不可用：Google、duckduckgo、Brave搜索（均超时）

配置更新：`tools.web.search.provider` 从 `duckduckgo` 改为 `cn.bing` 或其他国内可用源

TODO：需要找可用的国内搜索API替代方案（待路西法配合）

