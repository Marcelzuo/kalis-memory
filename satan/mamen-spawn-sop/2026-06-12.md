# 玛门调用 SOP + 误诊澄清 — 2026-06-12

## 概述
- 触发：老板 09:00 报告"玛门没响应，路西法/米迦勒没解决，靠撒旦"
- 老板反馈："确定解决问题再写入日志"（拒绝过早的"不用修"结论）
- 完工：09:12 修复闭环确认 + 日志补写
- 写入人：撒旦

## 初始误诊 → 重新诊断 → 真根因

### 1️⃣ 初始（错）误诊
- 看 `~/.claude/daemon.log` 最后 6-10 04:31 后静默 → 推断 Claude Code daemon 死了
- 推断 玛门 = sessions_spawn 链路是健康的
- 报告："不用修，链路是好的"
- **老板反驳**："确定解决问题再写入日志"

### 2️⃣ 重新诊断（深度实战）
- **4 步全链路测试**（spawn 一个玛门 subagent 跑 4 步）：
  1. `date "+%Y-%m-%d %H:%M:%S %Z"` → `2026-06-12 09:07:29 CST` ✓
  2. 读 AGENTS.md C.8 段 → 一字不差 ✓
  3. `df -h / | tail -1` → `460Gi 12Gi 124Gi 9%` ✓
  4. `git -C memory log --oneline -3` → 3 commits 一字不差 ✓
- 结论：sessions_spawn 链路 = 100% 健康
- daemon 路径 = 真的死了

### 3️⃣ 真根因（深挖启动必读后发现）
- workspace/AGENTS.md C.8 写对了"玛门 = sessions_spawn"
- **但玛门/米迦勒自己的"启动必读"文件没同步**：
  - `~/.claude/CLAUDE.md`（玛门启动必读）— 提"调度中心是撒旦 (OpenClaw)"，**没提 spawn 方式**
  - `~/.codex/AGENTS.md`（米迦勒启动必读）— 同上
- 玛门/米迦勒 启动时看不到"必须 sessions_spawn"
- → 撞 daemon 死路 → "玛门没响应" 是症状，真因是**调用方路径错**

## 修复

### 备份
- `~/.claude/CLAUDE.md.bak.20260612_090912` (5354 bytes)
- `~/.codex/AGENTS.md.bak.20260612_090912` (7530 bytes)

### 改动
- `~/.claude/CLAUDE.md` "## 🔗 启动必读" 段下加：
  ```markdown
  ### 玛门调用唯一路径（2026-06-12 立）
  **玛门 = OpenClaw `sessions_spawn` 唯一路径**。每次接受任务时必须确认调用方是用 sessions_spawn 派单（model 必传 `deepseek/deepseek-v4-pro`）。
  - ✅ 正确：撒旦/路西法用 `sessions_spawn(task, model="deepseek/deepseek-v4-pro")`
  - ❌ 禁止：直接调 `claude -p` / `hermes -z` 自己跑（依赖 Claude Code daemon，daemon 6-10 04:31 死了，这条路 0 命中）
  - ❌ 禁止：手动 `ANTHROPIC_AUTH_TOKEN=... claude ...` 调（绕开 OpenClaw 调度，无法被撒旦追踪）
  - 调用方报错"daemon 不响应/auth fail" → 立刻回贴"请改用 sessions_spawn"，不自己排错
  ```
- `~/.codex/AGENTS.md` "## 你的定位" 段下加同样内容（米迦勒视角措辞微调）

### 验证
- grep 验证两段都在 ✓
- 实战验证：spawn 玛门读自己 CLAUDE.md → 找到新规则（12-17 行），原样回贴一字不差 ✓

## 教训（三条）

1. **"问题"未必是"哪坏了"**——玛门"坏了"是路西法/米迦勒用了错误调用路径，不是玛门进程本身
2. **AGENTS.md 写对了不够**——必须同步到各角色的"启动必读"文件，才是真生效
3. **老板"确定再写日志"是 sharp 反馈**——别太快下"不用修"结论，深挖到根因再动笔

## 同步位置

| 位置 | 状态 |
|---|---|
| `memory/2026-06-12.md` | ✓ 09:00 + 09:12 节 |
| `MEMORY.md` | ✓ "玛门调用 SOP" 段 |
| 本文件 (`satan/mamen-spawn-sop/2026-06-12.md`) | ✓ 专题归档 |
| `~/.claude/CLAUDE.md` | ✓ 玛门启动必读已加 |
| `~/.codex/AGENTS.md` | ✓ 米迦勒启动必读已加 |
| shared 仓 `Marcelzuo/kalis-memory` | ⏳ 待 push |

## 老板拍板记录

- 09:00: "玛门没响应，路西法/米迦勒没解决，靠撒旦你了，过程不用问我"
- 09:05: "先看看玛门修复没有呢？" → 让我实战级验证
- 09:10: "确定解决问题再写入日志" → 拒绝过早"不用修"结论，倒逼深挖真根因
- 09:12: 闭环确认 → 准许写日志
