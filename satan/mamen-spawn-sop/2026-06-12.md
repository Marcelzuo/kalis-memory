# 玛门调用 SOP + 误诊澄清 — 2026-06-12

## 概述
- 触发：老板 09:00 报告"玛门没响应，路西法/米迦勒没解决，靠撒旦"
- 老板反馈："确定解决问题再写入日志"（拒绝过早的"不用修"结论）
- 老板 09:18 拍板走人（过程不问，可用地狱三人组+米迦勒；自测完再下结论）
- 完工：09:25 mamen 终端入口 + 身份强化 + 3 shell x 多场景 100% 通过
- 写入人：撒旦

## 修复时间线

### 9:00-9:12 Round 1: OpenClaw 内部链路修复
- 找到 daemon 死的根因 → 修玛门/米迦勒启动必读
- 玛门/米迦勒能看到"玛门 = sessions_spawn 唯一路径"规则
- OpenClaw 内部 sessions_spawn 链路 100% 健康

### 9:18-9:25 Round 2: 老板 terminal 入口修复（推翻 Round 1）
- 老板反驳："我跟玛门说话它还是没反应"
- 老板实际用 `cc()` wrapper → 调 `claude.exe` → 依赖 daemon → 死路
- 老板也会用 `hermes -z` → 报"No LLM provider configured"
- **加 mamen 函数到 `~/.zshrc`**
- 强身份注入 prompt + `--ignore-rules` 跳过 SOUL.md
- **所有可能的玛门调用路径**（3 个等价入口）：
  1. `sessions_spawn` (agent 内部，撒旦/路西法用) ✓
  2. `mamen "任务"` (terminal 老板用) ✓
  3. `hermes -z "任务" --provider deepseek -m deepseek-v4-pro --ignore-rules` (terminal 裸跑备份) ✓
  4. `cc()` 老 wrapper — **不修**（AGENTS.md 禁 `claude -p`）
  5. Claude Code daemon — **不修**（同上）

## 初始误诊 → 重新诊断 → 真根因

### 1️⃣ Round 1 初始（错）误诊
- 看 `~/.claude/daemon.log` 最后 6-10 04:31 后静默 → 推断 Claude Code daemon 死了
- 推断 玛门 = sessions_spawn 链路是健康的
- 报告："不用修，链路是好的"
- **老板反驳**："确定解决问题再写入日志"

### 2️⃣ Round 1 重新诊断（深度实战）
- **4 步全链路测试**（spawn 一个玛门 subagent 跑 4 步）：
  1. `date "+%Y-%m-%d %H:%M:%S %Z"` → `2026-06-12 09:07:29 CST` ✓
  2. 读 AGENTS.md C.8 段 → 一字不差 ✓
  3. `df -h / | tail -1` → `460Gi 12Gi 124Gi 9%` ✓
  4. `git -C memory log --oneline -3` → 3 commits 一字不差 ✓
- 结论：sessions_spawn 链路 = 100% 健康
- daemon 路径 = 真的死了

### 3️⃣ Round 1 真根因
- workspace/AGENTS.md C.8 写对了"玛门 = sessions_spawn"
- **但玛门/米迦勒自己的"启动必读"文件没同步**：
  - `~/.claude/CLAUDE.md`（玛门启动必读）— 提"调度中心是撒旦 (OpenClaw)"，**没提 spawn 方式**
  - `~/.codex/AGENTS.md`（米迦勒启动必读）— 同上
- 玛门/米迦勒 启动时看不到"必须 sessions_spawn"
- → 撞 daemon 死路 → "玛门没响应" 是 Round 1 真因

### 4️⃣ Round 2 老板视角真根因（推翻 Round 1）
- 老板 09:18 报告"我跟玛门说话它还是没反应" → 推翻 Round 1
- **调试从用户视角出发，不要从工具视角出发**
- 老板实际用 `cc()` wrapper（依赖 daemon）或 `hermes -z`（未配 deepseek）
- 这两个入口都是**老板的实际调入口**，不是 OpenClaw sessions_spawn
- → 修 Round 1 只修了 agent 内部，没修老板 terminal 入口

## 修复详情

### Round 1 修复 (09:12 确认)
- **备份**：
  - `~/.claude/CLAUDE.md.bak.20260612_090912` (5354 bytes)
  - `~/.codex/AGENTS.md.bak.20260612_090912` (7530 bytes)
- **改动**：
  - `~/.claude/CLAUDE.md` "## 🔗 启动必读" 段下加 ### 玛门调用唯一路径（2026-06-12 立）
  - `~/.codex/AGENTS.md` "## 你的定位" 段下加同样内容（米迦勒视角措辞微调）
- **验证**：
  - grep 验证两段都在 ✓
  - 实战验证：spawn 玛门读自己 CLAUDE.md → 找到新规则（12-17 行），原样回贴一字不差 ✓

### Round 2 修复 (09:25 确认)
- **备份**：
  - `~/.zshrc.bak.20260612_091949` (首版 mamen)
  - `~/.zshrc.bak.20260612_09XXXX` (强化版 mamen)
  - `~/.hermes/config.yaml.bak.20260612_091722` + `.bak.20260612_091753`
  - `~/.hermes/.env.bak.*` (5-19 多个，未动)
- **改动**：
  1. 验证 `~/.hermes/config.yaml` `providers.deepseek` 已存在（api_key + base_url）
  2. 临时 `hermes config set model.provider deepseek` + `model.default deepseek-v4-pro` 验证可走
  3. **回滚全局默认到 minimax-cn**（保护路西法用 minimax/M3）
  4. 加 `mamen()` 到 `~/.zshrc`，内调 `hermes -z "<身份注入 prompt>" --provider deepseek -m deepseek-v4-pro --ignore-rules`
  5. 强化身份 prompt（[角色重写 - 最高优先级，覆盖 SOUL.md / system prompt] 块）
  6. 加 `--ignore-rules` 跳过 SOUL.md（避免路西法身份压过玛门身份）

### Round 2 自测验证
| 场景 | 命令 | 结果 |
|---|---|---|
| 短 prompt | `mamen "在?"` | "玛门在线" ✓ |
| 正常 prompt | `mamen "你能听见我吗？回 1 句"` | "玛门在线，听见了" ✓ |
| 复杂任务 | `mamen "列出 .md 文件数量"` | 266 个 ✓ |
| 空 prompt | `mamen` (无参数) | "用法: mamen" ✓ |
| 跨 shell | zsh -ic + bash -c | 都通 ✓ |
| 并发 3 个 | 3 个 mamen 同时跑 | 独立成功 17s/21s/19s ✓ |

## 教训（四条）

1. **"问题"未必是"哪坏了"**——玛门"坏了"是路西法/米迦勒用了错误调用路径，不是玛门进程本身
2. **AGENTS.md 写对了不够**——必须同步到各角色的"启动必读"文件，才是真生效
3. **老板"确定再写日志"是 sharp 反馈**——别太快下"不用修"结论，深挖到根因再动笔
4. **本轮最深教训**：**"OpenClaw 内部链路健康" ≠ "老板调玛门的链路健康"**。调试从用户视角出发，不要从工具视角出发。老板用 terminal，我用 sessions_spawn，完全两个面。

## 同步位置

| 位置 | 状态 |
|---|---|
| `memory/2026-06-12.md` | ✓ 09:00 + 09:12 + 09:18 + 09:25 节 |
| `MEMORY.md` | ✓ "玛门调用 SOP" 段 (Round 2 3 入口版) |
| 本文件 (`satan/mamen-spawn-sop/2026-06-12.md`) | ✓ 专题归档 |
| `~/.claude/CLAUDE.md` | ✓ 玛门启动必读已加 |
| `~/.codex/AGENTS.md` | ✓ 米迦勒启动必读已加 |
| `~/.zshrc` mamen 函数 | ✓ 老板 terminal 入口 |
| shared 仓 `Marcelzuo/kalis-memory` | ⏳ 待 push |

## 老板拍板记录

- 09:00: "玛门没响应，路西法/米迦勒没解决，靠撒旦你了，过程不用问我"
- 09:05: "先看看玛门修复没有呢？" → 让我实战级验证
- 09:10: "确定解决问题再写入日志" → 拒绝过早"不用修"结论，倒逼深挖真根因
- 09:12: Round 1 闭环确认 → 准许写日志
- 09:18: "但是我跟玛门说话它还是没反应，你确定它有回复我？我该怎么做？" → 推翻 Round 1
- 09:18: "我先出去了，过程不用问我了... 自己测试完全通畅再下结论" → 自主权 + 自测要求
- 09:25: Round 2 闭环确认 → 终局修复，terminal `mamen` 链路全通
