# 2026-06-02

## 代理切换系统上线（19:30-19:50）

### 架构：动态检测，互不干扰
- **撒旦 (Gateway)**：永远直连，任何情况下不经过代理
- **路西法 (hermes)**：wrapper 脚本检测 ClashX 7890 端口，有则走代理，无则直连
- **玛门 (claude)**：同上，独立 wrapper，互不影响

### 实现细节
- ClashX 配置：`~/.config/clash/config.yaml`（从 jiang.yaml 复制，28节点）
- hermes wrapper：`~/.openclaw/bin/hermes` → 转发到 `~/.local/bin/hermes`
- claude wrapper：`/opt/homebrew/bin/claude` → 转发到原始 claude.exe
- Gateway env wrapper：`~/.openclaw/service-env/ai.openclaw.gateway-env-wrapper.sh`（不设代理）
- 已删除 proxy.env 静态配置

### 验证结果
- 代理模式：Google 200 ✅ 路西法 ✅ 玛门 ✅ 
- 直连模式：DeepSeek 正常 ✅ 路西法 ✅ 玛门 ✅
- 中途中止 ClashX：不会卡死 ✅（gateway 永远直连）

### 关键教训
- Gateway 不能设 HTTP_PROXY，否则 ClashX 中途关掉会全体断联
- 每次 exec 调用时实时检测 ClashX 端口，不依赖启动时的环境变量

## 收工总结 23:16
- 代理切换系统：三兄弟完美运行，Gateway直连+hermes/claude wrapper实时检测，永不卡死
- 图片迁移OSS→VPS本地：445张/1.7GB，Nginx alias直读
- Cloudflare DNS+CDN部署完成，缓存预热433/434
- v7独立站部署：5社媒入口+三品类产品
- 老板批准的未来规则：图片上传前先压缩到300-800KB
- 老板转达：感谢三兄弟，干杯🍷
