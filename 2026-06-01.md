# 2026-06-01 日志

## 品牌邮箱开通
- hello@kalistorik.com 上线
- 服务商：Zoho Mail Lite 国际版，$12/年
- 国际节点，欧洲投递靠谱
- DNS 配置：MX (mx.zoho.com / mx2.zoho.com / mx3.zoho.com)、SPF、DKIM 全绿
- 可多端同步：web / iOS / Android

## Token 大出血事件
- 发现今天 DeepSeek 烧了 ¥49.54，172 请求 3576万 tokens
- 元凶：VSCode Claude Code 插件后台跑了 2.5h，带 --max-thinking-tokens 31999（32K）
- 该插件绕过 settings.json，命令行参数直传
- 修复：杀掉僵尸进程 PID 5820

## 双保险部署
- settings.json：maxThinkingTokens=8000，全模型 pro，444 锁死
- Stop hook：CC 退出自动 chmod 444
- ~/.zshrc：cc() wrapper（解锁→运行→锁死→kill）
- 以后用 `cc` 代替 `claude` 命令

## 玛门宕机 + 修复
- VSCode Claude Code 扩展再次启动 → 杀进程
- hooks 配置与 VSCode 扩展不兼容 → 移除 hook，仅靠 cc() wrapper
- VSCode CC 扩展彻底禁用，以后只用终端 `cc` 命令

## MiniMax M3 三人升级
- 今天刚发布，模型 ID：MiniMax-M3，1M 上下文，MSA 架构
- 路西法：config.yaml 2处 M2.7→M3，已实测通过
- 撒旦：catalog.json M2.7→M3，contextWindow 204800→1000000
- 玛门：settings.json 模型列表加入 MiniMax-M3（DeepSeek保持主力）

## CC-Switch 灾难
- CC-Switch 覆写 settings.json，清空 DeepSeek 配置
- 代理端口 15721 不启动，CC 卡死
- 暴露了完整 API Key
- 最终：强退 CC-Switch，重建完整配置，444 锁死
- 结论：CC-Switch 未适配 M3，禁止打开
- switch_mammon.sh 重写，用环境变量保护 API Key

## 独立站邮箱更新（20:03）
- `info@kalis-torik.com` → `hello@kalistorik.com`（3处：联系卡片 ×1、mailto ×1、JSON-LD ×1）
- 桌面源文件 + 服务器同步更新
- 验证：线上 curl 确认通过

## 邮箱配置补充（20:21）
- 网页端：mail.zoho.com，hello@kalistorik.com，正常收发
- Zoho Mail APP：需选国际节点 zoho.com，否则提示"账户不存在"
- 第三方客户端需生成 App Password（IMAP imap.zoho.com:993 / SMTP smtp.zoho.com:587）

## Contact 表单后端上线（21:42）
- 方案：Python HTTP 服务 + Zoho SMTP，不依赖第三方
- 服务器：`/opt/contact-mailer/server.py`，127.0.0.1:8081，systemd 守护
- Nginx：`/api/contact` → proxy_pass
- v6 改动：CSS 反馈样式 + feedback div + 六语 thanks/error + JS fetch handler
- 数据流：表单 POST → /api/contact → Zoho SMTP → hello@kalistorik.com
- 提交反馈：六语成功/失败提示，客户端即时显示
- 老板验证：线上邮件收发正常

## Contact 表单完整上线 🎉（21:55）
### 架构
- 后端：Python HTTP 服务 `/opt/contact-mailer/server.py`，127.0.0.1:8081，systemd 守护
- 邮件：Zoho SMTP (smtp.zoho.com:587)，hello@kalistorik.com 认证
- Nginx：`/api/contact` → proxy_pass
- 反馈：六语 thanks/error 提示，客户端即时显示

### 关键踩坑
1. **表单无 name 属性**：原 v5 只有 placeholder，FormData 抓空 → 玛门加 name 属性修复
2. **systemd nobody 用户**：Permission denied → 改 root + chmod 修复
3. **路西法设计→老板确认→玛门执行→撒旦验收**：四步走，铁律没破

### 数据流
客户填表 → JS fetch POST `/api/contact` → Python → Zoho SMTP → hello@kalistorik.com → 老板 Zoho Mail 收到

---

## 独立站事项清单（2026-06-01 更新）

### ✅ 已完成
- [x] 邮箱 hello@kalistorik.com 开通（Zoho Mail Lite）
- [x] 独立站邮箱更新（info→hello）
- [x] Contact 表单后端上线（Python + Zoho SMTP）
- [x] 表单前端 v6（六语反馈 + name 属性修复）
- [x] 线上完整链路验证通过

### ⏳ 待办
- [ ] 社交媒体页面同步邮箱（LinkedIn/Instagram/WhatsApp，等待上线）
- [ ] Zoho Forms 备用方案（如需要数据管理面板）
- [ ] 其他老板指示

## 今日总结（22:03）
完成了 Contact 表单从零到上线的完整链路。
路西法 v5 改造设计规范（design_spec_v5.md）待明天继续：P0 4项 + P1 4项 + P2 5项 + P3 4项。
老板指令：今天休息，明天再说。晚安。
